<h1>Edit Page</h1>

<% if @page.errors.any? %>
  <%= error_messages_for(@page) %>
<% end %>

<%= form_for(@page, :url => {:controller => '/admin/pages', :action => 'update', :id => @page.id}) do |f| %>
  <%= render(:partial => 'form', :locals => {:f => f}) %>
  <%= f.submit %>
<% end %>
