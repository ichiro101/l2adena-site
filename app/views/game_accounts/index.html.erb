<%= render(:partial => 'submenu') %>
<h1>Ingame Accounts</h1>

<% if @user_game_accounts.size == 0 %>
  <p>You currently do not have an ingame account, to create one, please click "Create a Game Account"
  link.</p>
<% else %>
  You currently have <%= pluralize(@user_game_accounts.size, "ingame account") %>.
  <% @user_game_accounts.each do |user_game_account| %>
    <div class="game-account box">
      <strong>Login:</strong> <span class="account-name"><%= user_game_account.login %></span><br />
          <%= link_to("Change Password", {:action => 'edit', :id => user_game_account.login}) %>
    </div>
  <% end %>
<% end %>
